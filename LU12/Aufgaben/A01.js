// Bereite die Autodaten so zu, dass nur gültige PS
// und Verbrauchswerte(Horsepower, Miles_per_Gallon) übrig bleiben


//Lade die Daten von https://storage.googleapis.com/tfjs-tutorials/carsData.json
async function runTF() {
}

// Extrahiere nur die Felder Horsepower und Miles_per_Gallon mit einer map-Funktion.
function extractData(obj) {
}

// Entferne ungültige Einträge mit einer filter-Funktion (kein null erlaubt).
// Gib die vorbereiteten Daten in der Konsole aus.
function removeErrors(obj) {
}

runTF();