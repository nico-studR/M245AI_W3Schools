// aufgabe2_mittel_mehrere_preise.js

// Gewichtung und Bias
const w = 0.1;
const b = 2;

// Liste von Hausgrößen
const hausgroessen = [50, 75, 100, 125, 150];

// ==== Deine Lösung ab hier ====

function vorhersageMehrere(groessen) {
    // TODO:
    // Durchlaufe alle Elemente in groessen
    // Berechne für jede Größe den Preis mit y = w * x + b
    // Gib z. B. aus: "Haus mit 100 m² kostet: 12 Mio"
    // Bonus: Sortiere die Ausgabe nach Preis
}

// Beispielaufruf (optional aktivieren):
// vorhersageMehrere(hausgroessen);
