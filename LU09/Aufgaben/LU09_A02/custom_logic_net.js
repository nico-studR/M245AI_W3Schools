// Aufgabe 2 – Eigene Logik definieren und trainieren
// Ziel: Das Netz soll eine selbst definierte Logikregel lernen ("mindestens eine 1 ergibt 1")

const brain = require('brain.js');
const net = new brain.NeuralNetwork();

// === Eigene Trainingsdaten definieren ===
// input: [1, 1], output: [0] falls deine Regel das so vorsieht

// Netz mit den eigenen Daten trainieren
// net.train([...]);

// === Drei Eingaben testen ===
// Beispiel: [0,0], [1,0], [1,1]

// Ausgabe mit console.log anzeigen
